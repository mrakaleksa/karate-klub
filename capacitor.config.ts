import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.obilic.karateklub',
  appName: 'Karate klub Obilic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
